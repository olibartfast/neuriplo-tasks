#pragma once

#include "neuriplo/tasks/core/vision/image.hpp"

namespace neuriplo_tasks {

using ColorFormat = vision::ColorFormat;
using Image = vision::Image;
using ImageView = vision::ImageView;
using Layout = vision::Layout;
using PixelType = vision::PixelType;
using Size = vision::Size;

using vision::pixelTypeSize;

} // namespace neuriplo_tasks
